document.addEventListener('slideControl', () => {

    // const event = new CustomEvent('slideControl');
    // document.dispatchEvent(event);
   /* const twoRowsSliders = document.querySelectorAll('.cards_custom_slider.custom_slider_two_rows')

    if (twoRowsSliders && twoRowsSliders.length) {
        let countTwoRowsSliders = twoRowsSliders.length
        for (let len = 0; len < countTwoRowsSliders; len++) {
            const slidesContainer = twoRowsSliders[len].querySelectorAll('.custom_slider_slides>div')
            let countSlidesContainers = slidesContainer.length
            while (countSlidesContainers--) {
                const items = slidesContainer[countSlidesContainers].children
                if (items.length <= 1) continue;
                slidesContainer[countSlidesContainers].removeAttribute('style')
            }
        }
    }*/
});
